#include<stdio.h>
int main()
{
    int num[10],i,max;
    printf("enter 10 number :");
    max=num[0];
    for(i=0;i<=9;i++)
    {
        scanf("%d",&num[i]);
     if(max < num[i])
        max=num[i];
    }
    printf("greatest number is %d",max);
    return 0;
}
