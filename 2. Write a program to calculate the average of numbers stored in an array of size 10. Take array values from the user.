#include<stdio.h>
int main()
{
    int num[10],i,s=0;
    printf("enter 10 number :");
    for(i=0;i<=9;i++)
    {
        scanf("%d",&num[i]);
        s=s+num[i];
    }
    float avg;
    avg=s/10.0;
    printf("Average of 10 number is %f",avg);
    return 0;
}
