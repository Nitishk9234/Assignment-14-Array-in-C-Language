#include<stdio.h>
int main()
{
    int num[5],i,min;
    printf("enter 10 number :");
    min=num[0];
    for(i=0;i<=4;i++)
    {
        scanf("%d",&num[i]);
     if(min > num[i])
        min=num[i];
    }
    printf("smallest number is %d",min);
    return 0;
}
