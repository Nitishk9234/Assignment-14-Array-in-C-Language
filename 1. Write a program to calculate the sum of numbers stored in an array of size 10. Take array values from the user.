#include<stdio.h>
int main()
{
    int num[10],i,s=0;
    printf("enter 10 number :");
    for(i=0;i<=9;i++)
    {
        scanf("%d",&num[i]);
        s=s+num[i];
    }
    printf("sum of 10 number is %d",s);
    return 0;
}
